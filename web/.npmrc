# next-on-pages runs its own `npm install` during the Cloudflare build; this
# lets it resolve next@15.5.23, which sits above the adapter's declared peer
# range (<15.5.2). We pin 15.5.23 on purpose (CVE-2025-66478 fix).
legacy-peer-deps=true
